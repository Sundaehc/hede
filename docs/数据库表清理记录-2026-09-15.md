# 数据库表与定时任务清理记录

## 本次清理

以下对象已从业务运行层移除：

- `product_code_mappings`
- `master_data_entities`
- `master_data_aliases`
- `v_product_code_mappings`
- `v_master_data_aliases`
- Windows 计划任务 `HedeMasterDataSync`

对应的主数据回填、每日同步脚本和数据库模型注册也已移除，后端启动及数据库维护脚本不会再自动创建这些对象。

`scheduled_task_statuses` 与 `scheduled_task_runs` 中已有的执行历史保留不动，仅用于审计，不代表任务仍然存在或会继续执行。

生产数据库本次已通过事务性 SQL 完成删除；Alembic 迁移 `20260915_0067` 同步保留为后续环境的标准清理记录。当前数据库此前存在迁移版本漂移，直接执行全量 `alembic upgrade head` 会在旧的已存在表上被历史迁移阻断，因此未强行修改其他迁移或迁移版本。该清理不可逆，执行前应确认不再需要这些历史映射数据。
